<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/styles/styles.css" />
    <title>Blogs</title>
  </head>
  <main>
    <!-- Edit Post-->
    <!-- Edit Post Button-->
    <form action="/edit" method="POST">
      <input type="hidden" name="id" value="<%= posts.id %>" />
      <button type="submit" value="OK">Edit Post</button>
    </form>
    <!-- Edit Post Form -->
    <div class="editPost">
      <form id="editForm" action="/edit" method="POST">
        <!-- Textarea for title -->
        <label for="title">Title</label>
        <input type="text" name="title" value="<%= posts.title %>" />
        <!-- Textarea for creator Name -->
        <label for="CreatorName">Creator Name:</label>
        <input
          type="text"
          name="creator_name"
          value="<%= posts.creator_name %>"
        />
        <!-- Time -->
        <input
          type="date_created"
          name="date_created"
          value="<%= posts.date_created %>"
        />
        <!-- Textarea for the content -->
        <label for="body">Content:</label>
        <textarea name="body"><%= posts.body %></textarea>
        <!-- Update Button -->
        <button type="submit" value="OK">Update Post</button>
      </form>
    </div>
  </main>
</html>
